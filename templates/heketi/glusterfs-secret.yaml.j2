apiVersion: v1
kind: Secret
metadata:
  name: heketi-secret
  namespace: default
data:
  # base64 encoded password. E.g.: echo -n "mypassword" | base64
  key: {{ gs_heketi_secret_base64.stdout }}
type: gluster.org/glusterblock
