apiVersion: v1
kind: Endpoints
metadata:
  name: {{ gluster_registry_endpoint_name }}
subsets:
- addresses:
{% for host in groups['infranodes'] %}
  - ip: {{ hostvars[host]['ansible_eth0']['ipv4']['address'] }}
{% endfor %}
  ports:
  - port: 1
    protocol: TCP
